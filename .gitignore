# Dependencies
node_modules/

# Capacitor — subir código fuente nativo pero no los builds
android/build/
android/.gradle/
android/app/build/
ios/build/
ios/Pods/
ios/.xcode.env.local

# Build outputs
.next/
out/
/build

# Env files — nunca subir credenciales
.env
.env.local
.env.*.local

# OS
.DS_Store

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Yarn / pnpm
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.env*
